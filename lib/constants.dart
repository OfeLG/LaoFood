// Aquí se tienen los colores a utiliza en la app.

// Se importa el paquete material.dart
import 'package:flutter/material.dart';

// A los colores se le asignó unas constantes para que fuese facil de llamar en el codigo.

//Este será el color principal de la App
const primaryColor = Color(0xFF9CCC65); //0xFFAED581

const secondaryColor = Color(0xFFFF7043); //0xFFFFAB91

//Este será el color del fondo de la App (O sea, nuestro Background)
const backgraundApp = Colors.white;

//Este será en color del texto de la App
const textColor = Colors.grey;
